compare.v - compares to find the max number
next.v    - iterates until the spaces specified are run in the ROM
mem.v     - module that deals with the ROM
datapath.v- deals with the connection between 'compare' and 'next'
fsm.v     - controls the states for the simulation
top.v     - manages the connection between the three main modules: fsm, datapath, and mem. 